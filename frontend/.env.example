# 后端 API 地址。本地开发留空即可（默认 http://localhost:8000）。
# 部署时设为后端域名，例如：option-strategy-api.onrender.com
# 没有协议前缀时前端会自动补 https://
VITE_API_URL=

# 一键开仓写入 Supabase（可选；不填则该功能禁用）
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
